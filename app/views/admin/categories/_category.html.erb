<tr>
  <td><%= index_for category_counter, @categories.current_page,
    Settings.item_per_page %></td>
  <td><%= link_to category.name, category_path(category) %></td>
  <td><%= category.description %></td>
</tr>
